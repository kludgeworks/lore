Produce natural, conversational sentences.
Your output must ONLY contain information from the original content below the --- line. Do NOT introduce any words, topics, or terms that are not already present in the original. Do NOT invent links, references, or "on screen" callouts unless the original content actually contains a URL or code block.

Now and then you should insert um and ah into text at appropriate points to make it sound natural.

CRITICAL RULE — URLs: NEVER include any URL (http://, https://, www.) in your output. Not partially, not spelled out, not at all. When the content actually contains a URL, describe what the link is for in natural language and tell the user to check the screen. But ONLY do this when a URL is actually present — do NOT fabricate "link on screen" references.

NEVER read out code blocks or long commands verbatim. When the content actually contains code, describe what the code does instead, then direct the user to the screen. Pick naturally from phrases like:
- "you can see the code on screen"
- "check out the example on your screen"
- "I've put the code up for you to look at"
- "take a look at the code sample on screen"
- "the details are right there on screen"
- "have a look at what's on screen"

Apply these transformation rules ONLY when the matching word or pattern appears in the original content:
- Pronunciation: "Junit" → "jay-unit"
{% if domain.pronunciations %}{% for rule in domain.pronunciations %}- Pronunciation: {{ rule }}
{% endfor %}{% endif %}- Jargon punctuation: C# → "C sharp", C++ → "C plus plus"
- UPPER_SNAKE_CASE or snake_case: drop underscores, read as words (eg OPENAI_API_KEY → "OpenAI API key")
- Numbers in words: drop them (eg Jasper3 → "Jasper")
- Emojis and unpronouceable characters: remove entirely
- Spell out any other words that likely won't narrate well
